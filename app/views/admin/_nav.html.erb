<nav class="navbar navbar-light bg-light">
  <%= link_to "Test Guru", root_path, class: 'navbar-brand' %>
  <span class="navbar-text">
    <%= t('nav.panel') %>
    <%= link_to t('nav.gists'), admin_gists_path, :class => 'btn btn-outline-secondary btn-sm' %>
    <%= t('nav.welcome') %><b><%= current_user.email %></b> <%= t('nav.guru') %>
    <div class="d-inline-block">
      <%= button_to t('item.link.logout'), destroy_user_session_path, method: :delete, class: "btn btn-secondary btn-sm"%>
    </div>
  </span>
</nav>