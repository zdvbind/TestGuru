<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('nav.panel') %></span>

  <div class="nav-admin user">
    <%= t('nav.welcome') %> <b class="nav-admin user-email"><%= current_user.email %></b>
    <%= t('nav.guru') %> |
    <%= link_to t('item.link.logout'), destroy_user_session_path, method: :delete %>
  </div>
</div>