<div class="nav user">
  <%= link_to "Test Guru", root_path %>
</div>
  <% if user_signed_in? %>
    <div class="nav user">

      <%= t('nav.welcome') %><b><%= current_user.email %></b> <%= t('nav.guru') %>
    </div>
    <div class="nav user">
      <%= link_to t('item.link.logout'), destroy_user_session_path, method: :delete %>
    </div>
  <% else %>
    <div class="nav user">
      <%= link_to t('item.link.signup'), new_user_registration_path %>
    </div>
    <div class="nav user">
      <%= link_to t('item.link.login'), new_user_session_path %>
    </div>
<% end %>
